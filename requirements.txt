# Audio capture
sounddevice>=0.4.6

# STT (Vosk - lightweight, streaming, CPU)
vosk>=0.3.45

# GUI (narrow bar + settings). PySide6 required for X11 "reserve space" (strut via Qt's display).
PySide6>=6.6.0

# Optional: clipboard for "paste instead of type"
# (PySide6 provides QApplication.clipboard())
